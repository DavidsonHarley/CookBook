const React = require('react');
const Layout = require('./Layout');

module.exports = function Favorites({ newUser }) {
  return (
    <Layout newUser={newUser}>
      <h1>hi</h1>
    </Layout>
  );
};